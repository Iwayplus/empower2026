Options -MultiViews
RewriteEngine On

# Exclude /empower25
RewriteRule ^empower25(/|$) - [L]

# React SPA routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]